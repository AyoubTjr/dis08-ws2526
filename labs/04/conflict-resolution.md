## Merge conflict resolution: bio.md

When merging `feature-facts` into `main`, GitHub reported a merge conflict in `bio.md`.

Cause:
The same line in `bio.md` was edited differently on both branches (`main` and `feature-facts`).

Resolution:
I opened the conflict editor, removed the conflict markers, and wrote one final combined sentence.
Then I marked the file as resolved and completed the merge.

Outcome:
`facts.md` was merged successfully and `bio.md` now contains the final resolved text in `main`.
